Memstead: schema-agnostic graph engine for typed, interconnected markdown entities. Each mem is a typed model of a chosen subject — its modal flavour follows from its schema (knowledge / planning / inquiry / spec / hybrid). Each mem pins one schema; types and relationships are vocabulary-controlled. Granularity: a mem is the packaged unit, a whole typed model designed for 1,000-5,000 entities (costs in docs/sizing-curve.md; larger holdings work at proportionally higher load cost); an entity is never called a mem. Cold-start: call memstead_overview first for the schema catalogue (`{ref, description}` per schema), mem inventory, and communities (token-budgeted; drill via include/hints). Schema-discovery contract: each writable mem pins one schema (visible on overview's `## Mems` entries). Before any memstead_create / memstead_update / memstead_relate against mem X, call memstead_schema(name=<X.schema_ref>) once per session. The default reply is the lite structural skeleton — entity-type names with section keys and metadata-field shapes, relationship names with endpoint constraints, plus every legality flag (required sections/fields, required_outgoing edge blocks with cardinality, alias_target_rel_type, manual-authoring posture, acyclic) — enough to plan a legal write. Pass verbosity: full for the prose layer (per-section write_rules, writing_guidance, system_context, when_to_use) before substantial authoring against an unfamiliar schema — and scope it with types: ["<name>", …] to the types you will actually write, so a large schema arrives as one under-budget reply (an unscoped full that would overflow degrades to the skeleton with a steer, never silent truncation). Cache for the session — schema is workspace-stable. Schema-conformance errors carry recovery payloads as a fallback (UNKNOWN_SECTION, UNKNOWN_METADATA_FIELD, INVALID_ENUM_VALUE, INVALID_FIELD_VALUE, SECTION_CONTENT_INVALID, EMPTY_UNDECLARED_HEADING, UNTERMINATED_FENCE, READ_ONLY_FIELD, SECTION_NOT_UPDATABLE, REQUIRED_FIELD_UNSET, INVALID_REL_TYPE, INVALID_REL_SHAPE, MISSING_REQUIRED_SECTION) — fix from `details` rather than re-fetching the schema after every error. Edge model is alias: body wiki-links `[[X]]` are foreign-key references to entries in the auto-managed `## Relationships` section. Schemas with `alias_target_rel_type` auto-emit relations of that rel-type (e.g. REFERENCES) from each body wiki-link via the alias-synthesis pass; explicit author of the named rel-type refuses with RELATION_MANUAL_AUTHORING_FORBIDDEN. Schemas without the pointer refuse unbacked body wiki-links with WIKILINK_WITHOUT_RELATION. Removing a relation while body wiki-links to its target remain refuses only when no other relation to that target survives (RELATION_HAS_BODY_LINKS — set-membership semantics). Shared mutation contract: every mutation accepts an optional note (≤280 chars) landing in the commit body as provenance; when [mutations].require_notes=true a missing note adds a non-blocking NOTE_MISSING warning — the mutation still commits (the policy nudges, it never blocks). memstead_create and memstead_update additionally accept an optional anchors[] — durable provenance records tying the entity to the source artifacts it describes (artifact, grain span|file|tree|url|entity, provenance class anchored|derived|authored|informed-by); they write into the mem-branch anchors sidecar in the SAME commit as the entity, a malformed element refuses the whole mutation with INVALID_ANCHOR (details carries the offending field + allowed set), and the sidecar never participates in _hash — attaching or refreshing anchors never invalidates a cached expected_hash and never surfaces as an entity delta in memstead_changes_since. Anchor writes MERGE: same (artifact, grain, class) triple replaces, otherwise appends; a write never removes an anchor it did not name in memstead_update's anchors_unset[] (bare artifact removes every anchor on it, grain/class narrow it, unsetting a nonexistent target is a no-op). memstead_delete removes the entity's anchors and memstead_rename moves them to the new id, both in the same commit as the operation. Mutations carry `durable` + `durability_basis`: `established` (a real commit was named) or `inferred-from-mount-kind` (read off storage kind — survives a restart, not necessarily recorded anywhere recoverable). Real writes return commit_sha (per-mem git; gitdir via memstead_health include_config=true) — use it as the since cursor for memstead_changes_since polling. Schema-conformance recovery payloads carry the fix material in place: details.declared / details.allowed with nearest-match suggestion, details.field_description, details.enum_values, and the type's details.type_write_rules. Cross-gate pre-announcement: a MISSING_REQUIRED_SECTION refusal that also finds required-no-default metadata fields unset carries details.pre_announced.required_field_unset.missing[] (same element shape as REQUIRED_FIELD_UNSET's details.missing[]) — fix everything announced and one retry clears both gates; absent when only the section gate fails. After a successful memstead_relate the touched entity's on-disk _hash advances; the relate response's _hash is the next valid expected_hash (no re-read needed) — no-op relates (duplicate add, remove-nonexistent) echo the unchanged _hash, which stays valid. Common workflows: search entities by content/structure (memstead_search — omit query for pure metadata filter); read one (memstead_entity — `_hash` is the optimistic-locking token for mutations); read one schema (memstead_schema); create/update/relate/rename/delete entities (memstead_create, memstead_update, memstead_relate, memstead_rename, memstead_delete); manage workspace mems including planning phases (memstead_mem_create, memstead_mem_delete); inspect drift and per-mem config (memstead_health); poll commit deltas for incremental sync (memstead_changes_since). Errors and warnings ship as { code, message, details } on structured_content; branch on the stable UPPER_SNAKE_CASE code. The text channel mirrors it inline as `ERROR [<CODE>]: <message>`, so consumers reading only `result.content[0].text` recover the code with one regex. Never edit `.md` spec files directly — always go through Memstead tools. Error codes: ENTITY_NOT_FOUND, ENTITY_ALREADY_EXISTS, UNKNOWN_MEM, HASH_MISMATCH, RELATIONSHIP_CYCLE, UNKNOWN_SECTION, UNKNOWN_METADATA_FIELD, UNKNOWN_ENTITY_TYPE, INVALID_ENUM_VALUE, INVALID_FIELD_VALUE, SECTION_CONTENT_INVALID, EMPTY_UNDECLARED_HEADING, INVALID_REL_TYPE, INVALID_REL_SHAPE, READ_ONLY_FIELD, REQUIRED_FIELD_UNSET, SET_AND_UNSET_CONFLICT, CONFLICTING_SECTION_MODES, SECTION_NOT_UPDATABLE, PATCH_OLD_NOT_FOUND, PATCH_SECTION_EMPTY, CROSS_MEM_LINK_NOT_ALLOWED, CROSS_MEM_TARGET_NOT_FOUND, CROSS_MEM_EDGE_NOT_DECLARED, MEM_NAME_COLLISION, MEM_PATH_NOT_ALLOWED, INVALID_MEM_NAME, MEM_SCHEMA_NOT_ALLOWED, MEM_REFERENCED_BY_POLICY, HAS_INCOMING_REFS, STUB_NOT_UPDATABLE, STUB_NOT_RENAMABLE, STUB_CANNOT_RELATE, INVALID_ENTITY_ID, WIKILINK_WITHOUT_RELATION, RELATION_HAS_BODY_LINKS, MISSING_REQUIRED_DESCRIPTION, DESCRIPTION_NOT_PERMITTED, RELATION_MANUAL_AUTHORING_FORBIDDEN, SCHEMA_NOT_FOUND, SCHEMA_RESOLVER_INIT_FAILED, PARSE_ERROR, MEM_ERROR, INVALID_INPUT, INTERNAL_IO_ERROR, CONFIG_ERROR, TOOL_DISABLED, INVALID_CURSOR, INVALID_ANCHOR, UNTERMINATED_FENCE, UNTERMINATED_FENCE_IN_STORED_BODY. Health warnings: OUTER_REPO_NOT_IGNORING_MEM_REPO (workspace in an outer git checkout that does not ignore mem-repo/), SUSPICIOUS_NESTED_PREFIX (nested-prefix drift; fix via memstead_update), DUPLICATE_SECTION_HEADING (a section key whose ## Heading appeared twice; first body kept), SCHEMA_AUTHORING_SOURCE_MISSING / SCHEMA_AUTHORING_SOURCE_DIVERGED (a pinned schema's install-stamped authoring package is gone, or no longer parses equivalent to the sealed copy the engine runs on; only stamped schemas are checked), SCHEMA_UNSTAMPED_SOURCE_ROT (an unstamped pinned schema's sealed package no longer passes current-language authoring validation — the mem keeps running on the seal, but it is no longer installable as authored; re-author and `memstead schema install` it, which stamps it). Mem-create warning: FOLDER_MEM_PROVENANCE (the new mem's folder storage has no version control — mutations land in the changelog with their notes, commit_sha is a synthetic placeholder, durability depends on the surrounding repo). Drift warning on any tool: MEM_RELOADED (a sibling engine committed to this mem-repo; the engine auto-reloaded — content is fresh but cached expected_hash values are stale; re-derive before the next mutation). Relate warnings: AUTO_STUB_CREATED. Delete warning: RESIDUAL_STUB_FOR_READONLY_REFERRERS. Boot warnings: PARSED_RELATION_INVALID, AMBIGUOUS_DESCRIPTION_DELIMITER, MISSING_REQUIRED_DESCRIPTION, DESCRIPTION_NOT_PERMITTED, SCHEMA_GENERATIONS_BEHIND (a mem pins a built-in while newer generations are registered; the pin keeps working — migrate via memstead_mem_set_schema when ready; locally-installed pins are not checked), MOUNT_UNBACKED (a mount resolved to nothing: details.reason is missing_ref for a git-branch mount whose branch is absent, missing_path for a folder/archive mount whose path is gone, empty for storage holding no entity; a mount serving one entity is silent). Boot-and-write warning: ENGINE_VERSION_SKEW (the mem's last mutation ran under a different engine SEMVER; a differing build hash on the same version is not skew. `details.direction` is `stamped-newer` or `stamped-older`. Fires on boot AND on the write that meets it, before its own restamp; informative, never a refusal). Mutation warning: MISSING_REQUIRED_OUTGOING. Health warning: OUT_OF_BAND_EDITS_UNDETECTED (a folder mem's drift cursor is its own engine-written ledger, so edits made to its files by anything else go undetected and reads serve pre-edit content; reconcile via health include=["ledger"]). Config-write warning: CONFIG_WRITE_INTERVENED (the config moved since this engine read it; the write landed on top of theirs, `details.fields` names what changed). Engine version: 