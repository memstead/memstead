node_modules/
dist/
.astro/
.env
.env.production
.env.development
.DS_Store

# The reference pages (CLI, MCP, errors, parity, binding, WASM) are
# rendered from the engine sources of this checkout by
# `cargo run -p xtask -- generate-docs`, which scripts/prebuild.mjs runs
# as `prebuild`. A build output, never a committed copy.
src/content/docs/reference/

# Built from ../GLOSSARY.md (the normative source) by
# scripts/prebuild.mjs (runs as `prebuild`).
src/content/docs/glossary.md

# Generated from the plugin SKILL.md frontmatter by
# scripts/prebuild.mjs (runs as `prebuild`).
src/content/docs/skills.md
